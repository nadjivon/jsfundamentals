let studentName = 'naTASHA'
let capNAme='';
for ( let letter in i studentNAme) {
    if (n == 0){
        capName += studentName[letter].toUpperCase()
    } else {
        capName = capName + studentName[N]
    }
}