// * literals- values meant to be exactly as they are they are.

// * Javascript Syntax defines two tyoes of values: FIxed and varialbles values.
//* Fixed values are called literals.
//* Variable values are called variables.

let days = ['Monday' 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];
console.log(days.toString());
console.log(days.length);

let tired = true;

let shoup ={
    a:'chicken noodle',
    b:'tomato',
    c:'beef and barley'
};

console.log9soup.c)

let car = 'Honda';

let dec = 10.2;